# Full URL of the deployed backend API, including the /api suffix.
# Leave unset for local dev (Vite proxies /api to http://localhost:5000 — see vite.config.js).
VITE_API_URL=https://your-backend.onrender.com/api
