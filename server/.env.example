PORT=5000
MONGO_URI=mongodb://127.0.0.1:27017/tailor_boutique
JWT_SECRET=change_this_to_a_long_random_secret
CLIENT_URL=http://localhost:5173
NODE_ENV=development

# --- Production (Render/Railway) values ---
# MONGO_URI=<your MongoDB Atlas connection string>
# CLIENT_URL=https://your-frontend.vercel.app   (no trailing slash)
# NODE_ENV=production
# PORT is injected automatically by Render/Railway — no need to set it
